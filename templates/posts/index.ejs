<div id="posts">
<% posts.forEach( function( post ) { %>
    <div class="post">
        <h2 class="title"><%= post.title %></h2>
        <div class="body"><%= post.body %></div>
    </div>
<% }); %>
</div>

